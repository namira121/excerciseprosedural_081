#include <iostream>
using namespace std;

int main() {
	int nilaiM, nilaiB, rerata, TotalLulus, TotalGagal;
	char nama, status;

		cout << "====Nilai====" << endl;

		cout << "Masukkan Nilai Matematika : ";
		cin >> nilaiM; 
		cout << "\nMasukkan NIlai Bahasa Inggris : ";
		cin >> nilaiB;

		rerata = (nilaiM + nilaiB) / 2;
		if (rerata >= 70 || nilaiM > 80)
			cout << (status, "LULUS");
		else
			cout << (status, "Gagal");
		

		cout << "\nMasukkan Nama : ";
		cin >> nama;

		cout << nama << status;

		TotalLulus = 
		
		

}